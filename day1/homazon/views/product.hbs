<h2 class="center"><img src="{{user.pictureURL}}"/> Logged in as {{user.username}}</h2>
<div class= "all-products col-xs-10 col-xs-offset-1">
	<legend></legend>
  <div class = "single-product">
    <div class = "image-container col-xs-3">
      <img src="{{product.imageUri}}">
    </div>
    <div class = "words col-xs-7">
      <div class = "title">
        <h3>{{product.title}}</h3>
      </div>
      <div class = "description">
        <h5>{{product.description}}</h5>
      </div>
    </div>
    <div class = "col-xs-2 price">
      <h4>${{product.price}}</h4>
    </div>
    <form action="/cart/add/{{product._id}}" method="POST" style=>
      <button type="submit" class="btn btn-success">Add</button>
    </form>
  </div>
  <legend></legend>
</div>

<legend></legend>
<div class="center">
	<a class="btn btn-primary" href="/">Back</a>
	<a class="btn btn-primary btn-danger" href="/logout">Logout</a>
</div>

<style type="text/css">
.sent {
  text-align: right;
  color: #00B7FF;
}

.row {
  margin: 20px 0 !important;
}

.center {
  text-align: center;
}

.all-products {
}

.single-product {
  height: 200px;
  width: 100%;
  border: 3px;
}

.image-container {
  height: 100%;
  width: 100%;
}

img {
    max-width: 100%;
    max-height: 100%;
}

.price {
  text-align: right;
  vertical-align: middle;
  margin-top: 5%;
}
</style>